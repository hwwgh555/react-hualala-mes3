{
	// Place your global snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and 
	// description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope 
	// is left empty or omitted, the snippet gets applied to all languages. The prefix is what is 
	// used to trigger the snippet and the body will be expanded and inserted. Possible variables are: 
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. 
	// Placeholders with the same ids are connected.
	// Example:
	"react-redux-ajax-action.type": {
		"scope": "",
		"prefix": "react-redux-ajax-action.type",
		"body": [
			"// 获取仓库列表",
			"export const ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_START = '${1: WORK_SITE}_${2: GET} >> house list start'",
			"export const ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_SUCCESS = '${1: WORK_SITE}_${2: GET} >> house list success'",
			"export const ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_FAIL = '${1: WORK_SITE}_${2: GET} >> house list fail'",
		],
		"description": "UPPERCASE way to define the action type name"
	},
	"react-redux-ajax-store": {
		"scope": "",
		"prefix": "react-redux-ajax-store",
		"body": [
			"// 获取仓库列表",
			"[${1: WORK_SITE}_${2: GET}_HOUSE_LIST_START]: ($$state) => {",
			"    return $$state.set('loading', true)",
			"},",
			"[${1: WORK_SITE}_${2: GET}_HOUSE_LIST_SUCCESS]: ($$state, action) => {",
			"    return $$state.set('loading', false).merge({ ${4: keyname}: action.payload })",
			"},",
			"[${1: WORK_SITE}_${2: GET}_HOUSE_LIST_FAIL]: ($$state) => {",
			"    return $$state.set('loading', false)",
			"},",
		],
		"description": "UPPERCASE way to define the action type name"
	},
	"react-redux-ajax-action.payload": {
		"scope": "",
		"prefix": "react-redux-ajax-action.payload",
		"body": [
			"// 获取仓库列表",
			"export const ${4: WORK_SITE}List = createAction(${1: WORK_SITE}_${2: GET}_HOUSE_LIST_START, async (opts) => {",
			"    await axios.post(urlWMS, {",
			"        service: wms_SERVICE,",
			"        method: wms_getHouseUrl,",
			"        type: 'post',",
			"        data: opts,",
			"    }).then((res) => {",
			"        if (res.success) {",
			"            const data = JSON.parse(res.data)",
			"            dispatch({",
			"                type: ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_SUCCESS,",
			"                payload: data.records,",
			"            })",
			"        } else {",
			"            dispatch({",
			"                type: ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_FAIL,",
			"            })",
			"            message.warning(res.msg)",
			"        }",
			"   }).catch(() => {",
			"        dispatch({",
			"            type: ${1: WORK_SITE}_${2: GET}_HOUSE_LIST_FAIL,",
			"        })",
			"        message.warning('请求出错!~')",
			"   })",
			"})",
		],
		"description": "create the action to get the ajax rst as action's payload to modify the store"
	},
}
